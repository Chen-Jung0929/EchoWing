fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
numpy>=1.26.0
torch>=2.1.0
torchaudio>=2.11.0
soundfile>=0.12.1
# MP3/M4A/WEBM/AAC decoding uses system ffmpeg (not torchaudio.load / torchcodec).
# Windows: choco install ffmpeg  |  macOS: brew install ffmpeg
pydantic-settings>=2.2.0
httpx>=0.27.0
pandas>=2.1.0

# Perch v2 CPU SavedModel (XLA) requires TF 2.20+ and matching protobuf.
tensorflow-cpu>=2.20.0,<2.21.0
protobuf>=6.31.1,<8.0.0

# Legacy ONNX backend (TRIAGELENS_INFERENCE_BACKEND=onnx)
onnxruntime>=1.17.0
deep-translator
wikipedia>=1.4.0
